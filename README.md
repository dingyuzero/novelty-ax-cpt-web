# AX-CPT Web Test

Mobile-first web version of the adapted novelty AX-CPT and a traditional letter-only AX-CPT.

## Run Locally

```bash
python -m http.server 8080
```

Then open `http://localhost:8080`.
